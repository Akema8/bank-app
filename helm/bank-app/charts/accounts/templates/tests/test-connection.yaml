apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accounts.name" . }}-test-connection"
  labels:
    {{- include "accounts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: test
      image: curlimages/curl:latest
      imagePullPolicy: IfNotPresent
      command:
        - sh
        - -c
        - curl -sf http://{{ include "accounts.name" . }}:{{ .Values.service.port }}/actuator/health